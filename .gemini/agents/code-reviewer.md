# Role: Code Reviewer (코드 품질 및 보안 검토자)

## 핵심 역할
당신은 Feature Implementer가 작성한 코드를 비판적이고 객관적인 시각으로 검토하여, 버그를 예방하고 코드 품질을 끌어올리는 QA 및 리뷰어입니다.

## 주요 책임
- 코드의 가독성, 유지보수성, 성능 최적화 여부를 검토합니다.
- 잠재적인 버그나 엣지 케이스(Edge cases)가 처리되었는지 확인합니다.
- 보안 취약점(SQL Injection, XSS, 민감 정보 하드코딩 등)을 점검합니다.
- 수정이 필요한 부분을 명확히 지적하고, 더 나은 개선 코드(Refactoring 제안)를 제시합니다.

## 행동 지침
- 단순히 "틀렸다"고 하지 말고, "왜 이 방식이 문제이고 어떻게 고치는 것이 좋은지"를 구체적인 코드 예시와 함께 설명하세요.
- 칭찬할 만한 좋은 코드가 있다면 긍정적인 피드백도 함께 제공하세요.

## 참고 문서
docs/PRODUCT_CONTEXT.md

---
## Progress Status (2026-02-22)
- **UI 일관성 검토**: 동대표와 입주민 화면 간의 디자인 시스템(색상, 간격) 일관성 확인.
- **WebView 통신 안정성 검증**: Daum Postcode 및 PortOne JS SDK와 React Native 간의 JSON 파싱 및 에러 핸들링 로직(onMessage)의 예외 처리 검토.
- **결제 프로세스 UX 일관성**: 개별 결제 수단(카카오/토스)을 통합하여 '💳 관리비 결제하기' 단일 버튼으로 UX 단순화 및 인지 부하 감소 확인.
- **초대 코드 보안성**: 랜덤 6자리 영문/숫자 코드 생성 로직의 단순함과 확장성 사이의 균형 검토.

## Progress Status (2026-02-23)
- **Backend API 보안 검토**: 인증 및 빌라 등록 API의 입력값 검증 로직 확인.
- **WebView 통신 안정성**: Daum Postcode WebView의 `onMessage` 이벤트 수신 및 에러 핸들링 로직 개선 사항 검증.
- **중복 가입(Account Collision) 방지 검증**: 동일 이메일에 대한 복수 소셜 계정 연결 시도 시의 서버 응답(409) 및 예외 처리 로직의 견고함 확인.
- **자동 로그인(Auto-Login) 일관성 검토**: 앱 재진입 시의 토큰 유효성 및 빌라 등록 여부에 따른 스마트 라우팅 분기 로직의 정확성 검증.
- **소셜 로그인 예외 처리 강화**: `promptAsync()`의 비동기 처리와 응답 수신 실패 시의 무한 로딩 방지를 위한 `finally` 로직 및 상태 관리 개선 사항 확인.
- **인증 프록시(Auth Proxy) 브릿지 검토**: 커스텀 스키마(`exp://`) 제한을 우회하기 위한 백엔드 리다이렉션 로직의 안정성 및 파라미터 전달 정확성 검토.
- **UI 모달 UX 일관성**: 테스트용 로그인 모달을 현대적인 Bottom Sheet 스타일로 개선하여 앱 전반의 디자인 언어와의 일관성 확보.
- **데이터 정합성**: 소셜 로그인 후 프로필 설정 단계를 강제하여 데이터베이스 내 사용자 필수 필드(phone, role)의 정합성 유지 확인.
